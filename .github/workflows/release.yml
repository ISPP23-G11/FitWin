name: Release

on:
  push: 
    tags:
      - '*'

jobs:
  buildTest:
    uses: ISPP23-G11/FitWin/.github/workflows/main.yml@main
    secrets:
      CODACY_PROJECT_TOKEN: ${{secrets.CODACY_PROJECT_TOKEN}}    
     
  release:
    needs: buildTest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Release
        uses: softprops/action-gh-release@v1
